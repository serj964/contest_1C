#функция которая отражает подматрицу относительно нечетной диагонали
#размера k у которой верхняя левая клетка имеет индекс (i, j) 
def f(a,i,j,k):
    """
    длянечетной k
    """
    for l in range(i-1, i+k-2):
        for t in range(j-1, j+k-2):
            s = a[l,t]
            a[l,t] = a[i+k-t+j-2, j+k-l+i-2]
            a[i+k-t+j-2, j+k-l+i-2] = s
    return a
    
#вызов функции
fp(mat,1,1,3)
